import 'package:biersommelier/components/ExploreTabList.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('ExploreBar Widget Test', () {
/*     testWidgets('Renders with default values', (WidgetTester tester) async {
      // Build our app and trigger a frame.
      await tester.pumpWidget(
        const MaterialApp(
          home: ExploreBar(),
        ),
      );

      // Verify that the ExploreTabBar is rendered.
      expect(find.byType(ExploreTabBar), findsOneWidget);

      // Verify that the default ExploreList for Bars is rendered.
      expect(find.byKey(const ValueKey('bar-0')), findsOneWidget);

    }); */

  });
}