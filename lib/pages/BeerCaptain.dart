import 'package:flutter/material.dart';

class BeerCaptain extends StatelessWidget {
  const BeerCaptain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeerCaptain'),
    );
  }
}